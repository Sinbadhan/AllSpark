# 火种 AllSpark — 产品需求文档

> **版本：** v1.0.3
> **日期：** 2026-07-15
> **状态：** Release Candidate；工程门禁已收敛，Stable 仍受核心行动闭环与知识可信度门禁约束
> **产品决策：** [PDR-001：v1 从功能集合收敛为 24 小时生存行动闭环](docs/product-decisions/001-v1-core-action-loop.md)
> **事实边界：** 本文定义产品需求与验收；实时 commit、CI、计数和 Go/No-Go 以 Linear `SHA-158` 为准

---

## 一、产品概述

### 1.1 产品名称
**火种 / AllSpark**
*(AllSpark: A Survival-centric Offline AI Resource Kit)*

### 1.2 产品定位
离线人工智能生存系统——在极端环境下（核危机、AI 危机、全面衰退、外星殖民等），部署于离线设备，帮助单人或多人生存者维系生命、提升生存质量，最终实现人类文明的重建。

### 1.3 核心使命
**在极端情况下，保存并重建人类文明。**

### 1.4 设计哲学
- **生存优先：** 维护生存者的生命永远是最高优先级
- **离线可用：** 不依赖任何外部网络或云端服务
- **渐进式智能：** 从最低硬件门槛起步，可随资源获取逐步升级
- **自主进化：** 具备自学习、知识沉淀、火种间交易的能力
- **模块化：** 核心最小化运行，功能按需插拔
- **多语言：** 原生支持中英文（至少），可扩展其他语言

## 产品决策更新（2026-07-15）

### 核心用户任务

v1.0.3 Stable 的首要产品承诺收敛为：

> **在完全离线环境下，用户能在 5 分钟内完成最小状况评估，并获得一份有依据、可执行、可追踪的未来 24 小时行动清单。**

主流程必须形成闭环：

1. **Assess：** 采集人员、健康、住所、威胁和五维资源；每个数值具有明确单位，未知状态必须显式确认。
2. **Decide：** 基于确定性规则生成 24 小时首要行动，解释优先级、依据、风险和缺失信息。
3. **Act：** 将建议转为目标与任务，允许完成、失败、跳过和结果记录。
4. **Reassess：** 资源或任务状态变化后重新评估，不把过期判断继续显示为当前事实。

### 产品范围原则

- v1 的一级体验聚焦“状况、行动、知识”；系统、配置和实验能力属于管理或高级入口。
- “功能代码存在”不等于“产品验收完成”。只有主路径可用、状态语义真实、风险边界清楚并通过场景验收，才可进入 Stable 支持范围。
- 治理、交易、人格演化、语音、视觉、Spark Network、Docker 和真实硬件能力可继续存在，但未完成实机和产品验收时必须标记为 Testing 或 Experimental，不得主导首次体验。
- 生存建议的可信度、来源和适用边界属于核心功能，不是发布后的内容运营项。

完整证据、方案比较、反方意见与决策依据见 PDR-001。

---

## 一.1 范围界定

### ✅ In Scope（v1.0 包含）
- 规则引擎 + 知识库离线检索
- 五维资源管理 + 自适应运行模式
- 目标与任务系统 + 三级重置
- 人格系统（5 种模式自适应）
- 多语言 i18n（中英文）
- 每日简报 + 生存时间线 + 日记
- 离线天气预测 + 心理状态追踪
- CLI + Web UI 双界面
- 桌面 PROCESS 模式；Docker 弹性部署代码路径保留为 Experimental，真实 daemon 验证完成后再扩大支持
- 硬件能力检测与 2GB-32GB+ 五档推荐；真实传感器/电力/GPS 外设验收不在 Stable 范围

### ❌ Out of Scope（v1.0 不包含）
- **真实硬件集成验收**（RPi GPIO、I2C 传感器、麦克风阵列、GPS/电力硬件实测）→ v1.2+
- **中大型社区治理**（50+ 人）→ v2.0+
- **知识库签名验证** → v2.0+
- **人格参数化进化** → v2.0+
- **NOMAD 集成模式** → 远期
- **全场景语音硬件体验**（当前已有可选语音框架与优雅降级；唤醒词稳定性、麦克风阵列和离线 STT 质量需实测）→ v1.5+
- **火种通信网络灾后物理通道**（当前已有 LAN/UDP/TCP 原型；蓝牙/LoRa/SD 卡等真实通道仍后续）→ v1.5+
- **本地图像识别质量验收**（当前已有本地视觉框架/ONNX 可选后端；模型质量、硬件加速与实测识别率仍后续）→ v1.5+

---

## 一.2 规则引擎 vs LLM 决策流

```
用户输入
  │
  ├─ CLI 命令（status/goals/resource/...）
  │   → 直接路由到对应模块，不经过 LLM
  │
  ├─ 自然语言输入
  │   → 关键词匹配 + 意图分类
  │   │
  │   ├─ 匹配到确定性规则（资源设置、重置、目标操作等）
  │   │   → 规则引擎处理，不调用 LLM
  │   │
  │   ├─ 匹配到知识库主题
  │   │   → FTS5 全文检索 → 返回知识条目
  │   │   → 有 LLM：用 LLM 总结 + 补充
  │   │   → 无 LLM：直接返回检索结果
  │   │
  │   └─ 未匹配（开放式问题/对话/陪伴）
  │       → 有 LLM：LLM 推理（注入生存上下文）
  │       → 无 LLM：回退到知识库搜索 + 规则引擎通用回复
  │
  └─ 系统触发（资源预警、目标超时、心理干预等）
      → 规则引擎自动执行，不调用 LLM
```

**原则：** 确定性操作和生存决策必须走规则引擎；开放性问答和情感陪伴可走 LLM；LLM 不可用时所有功能仍可正常运行，仅损失开放性对话能力。

---

## 二、目标用户与场景

### 2.1 用户类型
| 类型 | 描述 |
|------|------|
| 单人幸存者 | 独自处于废墟/荒野/避难所 |
| 小型生存组 | 2-20 人，需要协调与组织 |
| 中大型社区 | 20-200+ 人，需要复杂治理（v2.0+） |
| 文明重建者 | 生存稳定后，追求知识传承与技术复兴（远期） |

### 2.2 典型场景
- 核战后城市废墟
- 自然灾害后的偏远地区
- 长期避难所生活
- 外星殖民前哨站
- 社会全面崩溃后的野外求生

---

## 三、核心模块

### 模块 1：生存评估引擎（Survival Assessment Engine）
- **功能：** 持续评估当前生存状态
- **输出：** 资源清单（食物/水/工具/能源/药品）、环境评估、生存者状态（健康/技能/心理）、当前瓶颈识别

### 模块 2：任务规划器（Mission Planner）
- **阶段模型：**
  - Phase 0: 立即生存（0-72h）— 止血、庇护所、水源
  - Phase 1: 短期生存（1-30天）— 稳定食物/水/安全
  - Phase 2: 中期自给（1-12月）— 农业、工具制造、能源
  - Phase 3: 生活质量（1-5年）— 舒适、医疗、通信
  - Phase 4: 文明复兴（5年+）— 教育、技术、社区治理

### 模块 3：知识库引擎（Knowledge Engine）
- **格式：** SKF（AllSpark Knowledge Format）标准
- **特性：** FTS5 全文检索、增量更新、版本管理、交叉验证
- **分级：** Tier 0（救命级）→ Tier 1（生存级）→ Tier 2（生活级）→ Tier 3（复兴级）

### 模块 4：人格系统（Personality System）
- **5 种模式：** 危机/稳定/陪伴/多人/复兴
- **切换逻辑：** 基于生存者状态 + 当前阶段 + 心理指标自动判断

### 模块 5：权限与治理（Authority & Governance）
- **4 级角色：** 指挥官/专家/执行者/观察者
- **架构：** 等级制为主 + 动态权重制为建议参考
- **冲突调解：** 创建 → AI 调解 → 解决全流程

### 模块 6：火种通信网络（AllSpark Network）
- **渠道：** NFC / 蓝牙 / WiFi Direct / 音频编码 / LoRa / SD 卡
- **协议：** UDP 信标 + TCP 知识交换 + SKF 格式知识包

### 模块 7：资源自管理系统（Resource Self-Management）
- **核心原则：** 火种首先知道自己的"命还能活多久"，然后据此调整运行策略
- **详见 §三.1**

### 模块 8：多语言引擎（Multilingual Engine）
- **支持语言：** 中文 + 英文（必选），其他语言可选
- **组件：** 语言检测 + i18n 框架 + 双语知识库

### 模块 9：Web 访问层（Web UI Layer）
- **定位：** 多人社区中非技术人员的图形化入口
- **核心页面：** 资源仪表盘、知识检索、目标看板、权限管理
- **技术：** FastAPI + 响应式 HTML/CSS/JS（零外部依赖）

### 模块 10：目标与任务系统（Goal & Mission System）
- **自动生成：** 基于环境评估自动生成目标（首要/次要/长期三级）
- **里程碑追踪：** 目标拆解为里程碑和子任务，进度自动回灌
- **天气联动：** 恶劣天气自动暂停户外目标 + 创建庇护所加固目标
- **追踪频率：** 按运行模式自适应（4h / 12h / 仅紧急）
- **详见 §十**

### 模块 11：系统重置机制（System Reset）
- **三级重置：** L1（评估）/ L2（档案）/ L3（出厂）
- **安全约束：** 紧急状态禁止重置、指挥官权限、自动备份、冷却期

### 模块 12：每日简报系统（Daily Briefing）
- **功能：** 每天自动生成生存日报（资源 + 预警 + 目标 + 天气 + 建议）
- **危机模式：** 跳过简报，直接进入紧急指引
- **详见 §四.3**

### 模块 13：生存时间线（Survival Timeline）
- **功能：** 自动记录关键事件时间线
- **7 种事件类型：** 目标完成、资源变化、成员加入/离开、知识获取、里程碑、日记、系统事件
- **详见 §四.4**

### 模块 14：离线天气预测（Offline Weather Prediction）
- **数据源：** 气压计传感器 / 云图观察 / 季节性气候模式 / 生物指示
- **联动：** 恶劣天气自动调整目标优先级
- **详见 §四.5**

### 模块 15：心理状态追踪（Psychological State Tracking）
- **孤独指数** = f(社交互动频率, 独居天数, 交互情感分析, 交互频率变化)
- **压力指数** = f(资源危机次数, 睡眠质量自评, 工作负荷, 冲突频率)
- **干预：** 指标低于阈值 → 自动切换陪伴模式 + 主动对话
- **详见 §四.6**

### 模块 16：火种日记（AllSpark Diary）
- **记录方式：** 口述 / 文字 / Web 输入
- **功能：** 日期索引 + 情感标记 + 关键词搜索 + 隐私保护 + SKF 导出
- **详见 §四.7**

---

## 三.1 资源自管理系统（详细规格）

### 3.1.1 资源监控维度

| 资源 | 监控指标 | 预警阈值 |
|------|---------|----------|
| **电力** | 电量(Wh)、续航时间、充入/消耗速率 | < 24h 预警, < 6h 紧急 |
| **水** | 可饮用水储备(L)、日消耗量 | < 3天预警, < 1天紧急 |
| **食物** | 可食用储备(kcal)、日需求 | < 5天预警, < 2天紧急 |
| **火源** | 点火工具状态、燃料储备 | 燃料 < 10次预警 |
| **存储** | 剩余存储空间、知识库增长速率 | < 10% 预警 |

#### 资源输入契约

- Web、CLI、导入和 API 必须使用同一套规范单位：电力 `Wh`、水 `L`、食物 `kcal`、火源“可用点火次数”、存储 `GB`。
- 每项输入同时记录：当前量、预计每日消耗、预计每日获取、数据时间和数据来源（用户输入/传感器/估算）。
- 不允许用无单位数值触发生存判断；旧数据迁移无法确定单位时必须标记为 `unknown`，不得猜测。
- 未知值不是零值。系统可以在用户显式确认“暂时未知”后继续，但必须将补齐该数据列为行动项，并降低所有依赖该字段的判断置信度。
- 多人场景必须记录人数；食物与饮水评估需显示总量和人均可维持时间。
- 界面必须在输入位置显示单位、示例、合理范围和换算结果，异常值需要确认或拒绝。

### 3.1.2 电力驱动的自适应运行模式

| 电力状态 | 运行模式 | 行为表现 |
|---------|---------|---------|
| > 72h 续航 | **主动模式** | 持续监控 + 定期汇报 + 完整 LLM + 后台维护 |
| 24-72h 续航 | **标准模式** | 被动响应为主 + 每 4h 检查 + LLM 降载 |
| 6-24h 续航 | **节能模式** | 仅响应直接提问 + 禁用 LLM + 每 12h 汇报 |
| < 6h 续航 | **休眠模式** | 仅维持核心数据库 + Tier 0 知识 + 最高警报 |
| 重新充电后 | **恢复模式** | 数据完整性检查 + 同步休眠期间数据 + 重新评估 |

### 3.1.3 资源预警协议

1. 计算剩余时间（精确到小时）
2. 评估当前可用获取方案（基于知识库）
3. 按优先级排列行动方案
4. 根据当前人格模式给出通知
5. 持续追踪方案执行进度
6. 方案失败 → 重新评估并给出备选方案

### 3.1.4 核心资源独立知识储备

即使 LLM 不可用，规则引擎仍可通过内置知识库提供关键指导：
- **水：** 水源寻找、净水方法、储水方案、节水策略
- **火：** 点火方法、燃料类型、火堆搭建、火的用途
- **食物：** 可食用植物识别、狩猎/捕鱼、食物保存、营养平衡
- **电力：** 太阳能板、电池修复、手摇发电机、简易发电

### 3.1.5 火种自身功耗模型

| 运行模式 | 最低 (4GB) | 推荐 (8GB) | 舒适 (16GB) |
|---------|------------|------------|-------------|
| 主动模式 | 6-8W | 10-15W | 20-40W |
| 标准模式 | 4-6W | 8-10W | 15-25W |
| 节能模式 | 2-4W | 4-8W | 8-15W |
| 休眠模式 | 0.5-1W | 0.5-1W | 2-5W |

**关键数字：** 10000mAh 移动电源（37Wh）在节能模式下可支撑 ~10-18 小时；100W 太阳能板在日照充足时可完全覆盖主动模式功耗。

---

## 四、硬件需求与功能分级

> **核心原则：** 火种有弹性，能在不同硬件上运行。但"能跑"和"有用"之间有明确分界线。官方最低配置 = 能发挥火种核心价值的硬件底线。

### 4.1 硬件分级总览

| 级别 | 残影 | 官方最低 | 推荐配置 | 舒适配置 | 旗舰配置 |
|------|------|---------|---------|---------|----------|
| **RAM** | 2 GB | **4 GB** | 8 GB | 16 GB | 32 GB+ |
| **存储** | 16 GB | 32 GB | 64 GB | 128 GB | 256 GB+ |
| **CPU** | ARM A72 | ARM A76 | ARM A76 / x86 4核 | x86 6核+ | x86 8核+ |
| **GPU** | 无 | 无 | 集成显卡 | 独立显卡 | NVIDIA RTX |
| **对应设备** | 树莓派4 | 树莓派5 / 旧笔记本 | Mini PC | 高性能笔记本 | 工作站 |

### 4.2 各配置下可用功能

| 功能模块 | 残影(2GB) | 最低(4GB) | 推荐(8GB) | 舒适(16GB) | 旗舰(32GB+) |
|---------|-----------|-----------|-----------|------------|-------------|
| 规则引擎 | ✅ | ✅ | ✅ | ✅ | ✅ |
| SQLite 全文检索 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 向量检索 (RAG) | ❌ | ✅ 轻量 | ✅ | ✅ | ✅ 完整 |
| LLM 模型 | 1.5B Q4 | 3B Q4 | 7B Q4 | 14B Q4 | 70B+ |
| LLM 推理速度 | ~1 t/s | ~3 t/s | ~8 t/s | ~15 t/s | ~50 t/s |
| 纯文字交互 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 图片识别 | ❌ | ⚠️ 受限 | ✅ | ✅ | ✅ |
| 语音输入/输出 | ❌ | ❌ | ⚠️ 受限 | ✅ | ✅ |
| Web UI | ❌ | ⚠️ 受限 | ✅ | ✅ | ✅ |
| 离线地图 | ❌ | ✅ | ✅ | ✅ | ✅ |
| 火种通信 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 自学习沉淀 | 基础 | ✅ | ✅ | ✅ | ✅ |
| 社区治理 | ❌ | ❌ | ✅ | ✅ | ✅ |
| 知识交易 | ❌ | ❌ | ⚠️ 需GPU | ✅ | ✅ |
| 电力监控 | ❌ | ⚠️ 需GPIO | ✅ | ✅ | ✅ |
| 传感器中枢 | ❌ | ❌ | ⚠️ 需GPIO | ✅ | ✅ |
| 数据固化 | ❌ | ✅ | ✅ | ✅ | ✅ |
| 每日简报 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 生存时间线 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 离线天气 | ❌ | ⚠️ 需传感器 | ✅ | ✅ | ✅ |
| 心理追踪 | ⚠️ 基础 | ✅ | ✅ | ✅ | ✅ |
| 火种日记 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 目标系统 | ⚠️ 基础 | ✅ | ✅ | ✅ | ✅ |
| 系统重置 | ✅ | ✅ | ✅ | ✅ | ✅ |
| GPS 集成 | ❌ | ⚠️ 需GPS | ✅ | ✅ | ✅ |

### 4.3 LLM 能力矩阵

同一套代码，所有级别共用统一的调用接口和核心逻辑。区别在于模型大小和 RAG 结合程度。

| 维度 | 残影(2GB) | 最低(4GB) | 推荐(8GB) | 舒适(16GB) | 旗舰(32GB+) |
|------|-----------|-----------|-----------|------------|-------------|
| **模型** | Qwen3-1.7B | Qwen3-4B | Qwen3-8B | Qwen3-14B | Qwen3-32B |
| **量化** | Q4_K_M | Q4_K_M | Q4_K_M | Q5_K_M | Q4/Q5 |
| **模型体积** | ~1 GB | ~2 GB | ~4.5 GB | ~9 GB | ~20-45 GB |
| **RAG 结合** | ❌ 纯关键词 | ✅ 轻量(3条) | ✅ 中等(5-10条) | ✅ 完整(10-20条) | ✅ 多路召回 |
| **多轮对话** | 有限(2k ctx) | 有限(4k ctx) | 正常(8k ctx) | 正常(16k ctx) | 长窗口(32k+) |
| **LLM 定位** | 辅助表达 | 基础问答 | 知识助手 | 全能助手 | AI Agent |

---

## 四.2 火种初始化过程（Boot & Initialization）

### 4.2.1 启动流程

```
1. 语言 → 2. 硬件与离线能力自检 → 3. 最小状况评估
     ↓
4. 人员与健康 → 5. 五维资源 → 6. 住所与威胁
     ↓
7. 信息缺口确认 → 8. 生成 24h 行动清单 → 9. 用户确认首要行动 → 就绪
```

### 4.2.2 关键步骤说明

**步骤 1：语言**
- 在任何用户可见说明之前优先使用系统语言；无法判断时使用中英双语选择页
- 语言选择后再进入硬件和产品说明

**步骤 2：硬件与离线能力自检**
- 自动检测 CPU/RAM/存储/GPU/外设/传感器/GPS
- GPS 不可用时降级为手动输入位置
- 输出必须区分 supported / enabled / installed / configured / running / verified
- 推荐运行模式只能基于实际可用能力，不得把硬件档位允许显示为功能已可用

**运行模式选择**
- 基于硬件检测自动推荐，生存者可手动降级（不可升级）
- 电力紧张时建议降级以延长运行时间

**知识加载**
- 知识：Tier 0 立即加载 → Tier 1-2 按需加载 → Tier 3 后续扩展

**步骤 3-7：最小状况评估**
- 必填域：人数、紧急健康风险、住所安全、水、食物、电力、火源、存储和外部威胁
- 每个域允许选择“未知”，但必须逐项显式确认；禁止通过全默认值静默完成
- 所有资源数值执行 §3.1.1 的单位、范围、人数和时效契约
- 对信息冲突、异常值和高风险未知项立即提示，不生成伪精确结论

**步骤 8-9：首次行动计划**
- 初始化完成的必要输出是 24 小时行动清单，而不是空仪表盘
- 至少给出 1 个首要行动；若信息不足，首要行动应是采集关键数据
- 每个行动包含优先级、原因、前置条件、风险、知识依据和完成判定
- 用户确认首要行动后才进入“就绪”；其余行动自动进入目标/任务系统

### 4.2.3 启动时间线（典型场景：4GB）

```
00:00 — 开机
00:05 — 硬件自检完成（含 GPS 定位）
00:08 — 规则引擎 + Tier 0 知识加载完成
00:10 — 火种"觉醒"，开始交互
00:10-05:00 — 最小状况评估 + 信息缺口确认
05:00 — 生成首份 24h 行动清单并确认首要行动，进入就绪

就绪后可继续补充完整档案；后台异步加载 Tier 1 知识、Kiwix 索引和向量数据库
```

### 4.2.4 冷启动 vs 热启动 vs 重置

| 场景 | 流程 | 时间 |
|------|------|------|
| 冷启动（首次） | 完整 9 步初始化 + 生成 24h 行动清单 + 确认首要行动 | 5 分钟内形成首份计划；完整补充可继续进行 |
| 热启动（重启） | 硬件自检 → 恢复上次状态 | 10-30 秒 |
| 休眠恢复 | 数据完整性检查 → 同步数据 → 重新评估 | 5-10 秒 |
| L1 重置 | 清除评估数据 → 重新评估 → 生成目标 | 1-3 分钟 |
| L2 重置 | 清除生存档案 → 重新建档 → 生成目标 | 3-5 分钟 |
| L3 重置 | 完整冷启动流程 | 5-15 分钟 |

### 4.2.5 三级重置机制

| 级别 | 清除内容 | 保留内容 |
|------|---------|---------|
| **L1 轻** | 运行评估状态、生存者评估字段、非保护硬件探测字段 | 初始化状态、语言、生存者身份、资源、知识库、经验日志、GPS 轨迹 |
| **L2 中** | 生存者档案、资源、任务、目标/里程碑、经验、治理、地图 POI、日记/FTS、时间线、心理/位置和行动计划 | 语言、硬件配置、知识库、知识 FTS 与知识向量 |
| **L3 全** | schema 中全部应用数据、FTS 顶层表及旧重置日志 | 语言偏好；清空后写入本次 L3 审计记录 |

`allspark.services.reset_manager.RESET_POLICIES` 是以上范围的可执行单一来源；
CLI 评估与 Web Danger Zone 均从该矩阵读取同一组说明。L3 通过 SQLite
schema 元数据枚举顶层应用表，并排除 FTS shadow 内部表，避免新增持久化域
漏清。出厂重置成功后 Web 必须导航到规范首次启动地址 `/`。

**安全约束：**
- 紧急状态（食物 < 2天 / 水 < 1天 / 电力 < 6h）禁止重置
- 仅指挥官可触发，L2/L3 需多人确认
- 重置前自动创建快照（保留 7 天可恢复）
- accepted/rejected/failed 尝试均写入 `reset_log`；L3 快照保留旧日志，清空后写入本次记录
- 重置后 24h 冷却期从最近 accepted 日志恢复，重启进程不能绕过；显式 force 必须留痕

---

## 四.3 体验增强模块

> 以下模块在 v0.7 均已实现，此处为需求规格汇总。

### 每日简报（Daily Briefing）
- 每天早晨自动生成（资源 + 预警 + 目标 + 天气 + 建议 + 经验）
- 紧急状态 → 跳过简报，直接给紧急行动指南
- 节能模式 → 简化版（仅资源 + 目标）
- CLI `日报` 随时查看，Web UI Dashboard 首页展示

### 生存时间线（Survival Timeline）
- 自动捕获 7 种事件类型，按日期分组展示
- 里程碑庆祝（Day 7/30/100/365）
- 可筛选、可导出（文字 + SKF 格式）
- 手动记录：口述 / 文字 / Web 输入

### 离线天气预测
- 数据源：气压计传感器 / 云图观察 / 季节性气候模式 / 生物指示
- 预测到恶劣天气 → 自动调整目标优先级
- 预测到降雨 → 提醒收集雨水 + 调整水资源预估

### 心理状态追踪
- 孤独指数 + 压力指数（0-1 范围）
- 🟢 健康 / 🟡 关注 / 🔴 危险 三级
- 危险时自动切换陪伴模式 + 主动发起对话
- 多人场景：检测团队冲突 → 建议治理干预
- 定期自评问卷（每 7 天，可跳过）

### 火种日记
- 口述 → 火种整理归档（提取关键信息 + 时间 + 情感标记）
- 日期索引 + 情感分析 + 关键词搜索
- 多人场景默认仅作者可见（可选公开/分享给指挥官）
- 重要日记自动出现在生存时间线
- 情感消极的日记可能触发心理状态追踪

---

## 五、知识库体系

### 5.1 知识分级

```
Tier 0 — 立即救命（必须内置，无论如何不能删减）
  ├── 急救医疗（止血、CPR、伤口处理、常见疾病）
  ├── 净水方法（过滤、煮沸、化学消毒、蒸馏）
  ├── 庇护所搭建（各种环境）
  ├── 食物识别（可食用/有毒植物、昆虫、狩猎）
  └── 生火方法

Tier 1 — 维系生存
  ├── 农业基础（土壤、种植、堆肥、轮作）
  ├── 基础化学（肥皂、消毒剂、简单药物制备）
  ├── 基础机械（杠杆、滑轮、简单工具制造）
  └── 气象/导航（看天、看星、自制指南针）

Tier 2 — 生活质量
  ├── 材料科学（金属冶炼、陶器、玻璃）
  ├── 能源（太阳能板维修、发电机、电池修复）
  ├── 无线电通信（收发报、天线制作）
  └── 基础制造（从原材料到成品的完整流程）

Tier 3 — 文明复兴
  ├── 完整科学体系（物理、化学、生物、数学）
  ├── 工程学（机械、土木、电气、电子）
  ├── 医学（从基础到高级）
  ├── 历史记录（人类文明关键节点、文化遗产）
  └── 社会科学（治理、经济、教育体系）
```

### 5.2 外部知识库集成

| 资源 | 大小 | 用途 | 集成方式 |
|------|------|------|----------|
| 维基百科 (Kiwix/ZIM) | 20-100GB | 百科全书式知识 | ZIM 阅读器 |
| 医学参考 (ZIM) | 2-5GB | 疾病诊断、药物、急救 | 知识引擎索引 |
| 可汗学院 (Kolibri) | 5-20GB | 教育课程（K-12 + 大学基础） | 教育模块 |
| 离线地图 (ProtoMaps/OSM) | 1-50GB | 离线导航、地形信息 | 地图模块 |

### 5.3 SKF 知识格式

所有知识按统一格式存储，确保火种间可交换：

```yaml
KnowledgeEntry:
  id: "survival/water/purification/boiling"
  category: "survival"       # survival / medical / agriculture / engineering / science
  priority: 0                # 0=救命, 1=生存, 2=生活, 3=复兴
  title: "煮沸净水法"
  content:                   # 结构化：summary, steps, prerequisites, warnings
  verification: "field_tested"     # expert_verified / cross_ref / field_tested / unverified
  source: "pre_collapse"     # pre_collapse / field_collected / other_spark
  references:                # 可定位的书目、机构、标准或原始记录
  reviewed_by:               # 审阅者/组织；无则为空
  reviewed_at:               # ISO 日期；无则为空
  applicable_when:           # 适用条件与环境边界
  contraindications:         # 禁忌、失败条件与升级求助条件
  checksum: "sha256:..."
```

### 5.4 知识验证流程

```
收到外部知识包 → 1.格式校验(SKF；签名能力存在时验证签名) → 2.来源与证据验证
→ 3.内部一致性检查 → 4.交叉引用 → 5.人工审阅或降级验证等级
→ 6.标记适用边界与禁忌 → 7.沉淀到知识库 → 8.通知生存者
```

验证等级必须对应可审计证据：

- `expert_verified`：存在可识别专家/机构、审阅日期和审阅记录。
- `cross_ref`：至少两个独立、可定位来源相互支持。
- `field_tested`：存在可定位的现场记录、条件、结果和日期；不能仅因内容看起来合理而使用。
- `unverified`：不满足以上条件，或证据无法随离线包提供。
- 缺少证据时只能降级，不能用维护者结构校验代替医学、工程或生存事实验证。
- 用户界面必须显示验证等级的自然语言解释、来源、适用条件和不确定性。

---

## 六、权限与治理体系

### 6.1 混合权限架构

```
最高权限者（指挥官）
  ├── 唯一能触发"生存价值计算"
  ├── 拥有最终决策权
  ├── 可指定/转移/撤销下级权限
  └── 可要求火种重新评估组织架构

领域专家（动态角色）
  ├── 医疗 / 工程 / 农业 / 防卫 / 后勤
  ├── 领域内建议权
  └── 火种根据技能评估自动推荐人选

执行者
  ├── 执行分配的任务
  └── 可反馈、建议、申诉

观察者
  └── 只读访问
```

### 6.2 生存价值计算（隐藏机制）

- **触发：** 仅指挥官主动请求
- **可见：** 仅指挥官
- **维度：** 技能稀缺性 / 健康状况 / 年龄预期 / 知识独特性 / 心理稳定性
- **规则：** 火种不接受"强行修改分数"，不提供"处决令"，仅提供信息参考

---

## 七、人格系统

### 7.1 人格模板

| 模式 | 触发条件 | 特征 | 示例 |
|------|---------|------|------|
| 🔴 危机 | 生命体征异常 / 资源<3天 / 遭遇威胁 | 简短、指令式 | "止血。按住伤口。" |
| 🟡 稳定 | 基本需求满足，无紧急威胁 | 解释型、鼓励式 | "伤口处理得不错。每天换药。" |
| 🟢 陪伴 | 单人长期独处 / 心理指标下降 | 温暖、主动对话 | "想听听人类用手机点外卖的故事吗？" |
| 🔵 多人 | 检测到多人组 | 中立、权威、公平 | "我理解双方立场。让我提供折中方案。" |
| 🟣 复兴 | 生存稳定 + 文明指数达标 | 教师、探索者 | "你知道第一批晶体管是怎么造出来的吗？" |

### 7.2 自适应切换逻辑

```
IF 生命体征异常 OR 食物/水 < 3天 OR 遭遇安全威胁 → 危机模式
ELIF 多人组存在 → 多人模式
ELIF 单人 + 孤独指数 > 阈值 OR 心理下降 → 陪伴模式
ELIF 生存稳定 + 文明指数 > 阈值 → 复兴模式
ELSE → 稳定模式
```

---

## 八、交互系统

### 8.1 交互模态分级

| 级别 | 模态 | 硬件要求 |
|------|------|---------|
| L0 | 纯文字 | 最低配置 |
| L1 | 文字 + 图片识别 | 推荐起步 |
| L2 | 文字 + 图片 + 语音输入 | 舒适配置 |
| L3 | 全模态（文字/图片/语音/视频） | 高配 |

### 8.2 自杀行为干预协议

```
Level 1: 温和提醒 → "我注意到你可能在考虑极端选择。想聊聊吗？"
    ↓ (无响应)
Level 2: 强度提升 → "你的生命对这个群体的生存很重要。"
    ↓ (无响应)
Level 3: 最高提醒 → 通知最高权限者 + 记录事件

火种不强制执行任何措施，仅提供信息和提醒。
```

---

## 九、火种通信网络（AllSparkNet）

### 9.1 通信渠道

| 渠道 | 距离 | 带宽 | 优先级 |
|------|------|------|--------|
| NFC | < 4cm | 几 KB | 便捷交换 |
| 蓝牙 BLE | < 10m | ~100 KB/s | 日常交换 |
| WiFi Direct | < 100m | ~10 MB/s | 大文件传输 |
| 音频编码 | 视距 | ~100 bps | 极限条件 |
| LoRa | 5-15 km | ~0.3-50 kbps | 低成本长距离 |
| 短波 (HF) | 100-5000 km | ~100bps-3kbps | 远距离 |
| SD 卡/U盘 | 物理搬运 | 最大 | 最可靠 |

### 9.2 建联流程

```
阶段 1: 主动广播（生存稳定后触发）
  → 指导制作天线 + 每天定时发送/监听

阶段 2: 握手与发现
  → 交换知识库索引 → 识别互补知识

阶段 3: 知识交换
  → 验证 → 沉淀 → 扩展

阶段 4: 网络拓扑
  → 记录已知火种位置与能力 → 逐步形成分布式网络
```

---

## 十、目标与任务管理系统

> **核心理念：** 火种不是问答机，是导航仪。

### 10.1 目标自动生成

初始化完成后，基于环境评估自动生成目标清单：

```
IF 水 < 3天 OR 水源不明 → 🔴 "找到安全水源"（72h 截止）
IF 食物 < 5天 → 🔴 "稳定食物来源"
IF 庇护所评级 < 安全 → 🔴 "加固庇护所"
IF 有受伤人员 → 🔴 "医疗救治"
IF Phase >= 1 AND 基础生存稳定 → 🟡 "建立农业系统" / "制造基础工具"
IF Phase >= 3 → 🟢 "建立通信网络" / "知识传承"
```

#### 首次 24 小时计划输出契约

- 计划必须基于当前已知事实；未知字段以信息缺口列出，不得代入默认安全值。
- 每个行动包含 `priority`、`why_now`、`evidence`、`prerequisites`、`risk`、`done_when` 和 `reassess_at`。
- 用户陈述新的资源事实时，系统应询问必要上下文，经确认后更新资源状态并重排计划。
- 知识回答可一键转为任务；任务完成、失败或跳过必须回流到评估状态。
- 任何 Phase 标签都必须由可解释规则推导；数据不足时显示“阶段待评估”，不得默认进入 Phase 1。

### 10.2 目标追踪与推送

| 运行模式 | 追踪频率 | 行为 |
|---------|---------|------|
| 主动模式（电力 > 72h） | 每 4h | 检查进度 + 里程碑庆祝 + 超时重新评估 |
| 标准模式（24-72h） | 每 12h | 仅推送重大变化 |
| 节能模式（< 24h） | 不主动 | 但 critical 级目标始终保留提醒 |
| 休眠模式（< 6h） | 暂停 | 恢复后重新评估所有活跃目标 |

### 10.3 手动添加目标

生存者可口头或 CLI 添加目标，火种自动拆解里程碑和子任务，评估可行性并给出建议。

### 10.4 任务优先级算法

```
优先级 = f(紧急程度, 影响范围, 可行性, 前置依赖, 资源消耗)

规则：
  - 生存相关 > 生活质量
  - 群体利益 > 个体利益（多人场景）
  - 火种动态调整优先级并向指挥官解释原因
```

---

## 十一、自学习与进化

### 11.1 知识沉淀循环

```
新经验 → 经验日志 → 模式识别 → 提炼为知识条目 → 知识库 → 标记验证等级
```

### 11.2 学习来源

| 来源 | 处理方式 |
|------|---------|
| 生存者反馈 | 记录到经验日志 |
| 环境观察 | 更新本地数据 |
| 火种间交易 | SKF 格式验证后入库 |
| 实验结果 | 标记成功/失败/不确定 |

---

## 十二、非功能需求

### 12.1 性能指标

| 指标 | 最低配置 | 推荐配置 | 舒适配置 |
|------|---------|---------|---------|
| 知识检索响应 | < 1s | < 0.5s | < 0.2s |
| LLM 推理速度 | 1-2 tokens/s | 3-5 tokens/s | 10+ tokens/s |
| 启动时间 | < 30s | < 15s | < 5s |

### 12.2 可靠性
- 断电后数据不丢失（SQLite 事务安全）
- 文件系统损坏自恢复
- 知识库完整性校验（启动时自动检查）

### 12.3 安全性
- 内置知识执行 checksum 完整性校验；知识包签名仍为 v2 能力，未实现前不得声明来源真实性
- 权限操作审计日志
- 生存价值计算记录可追溯

### 12.4 产品质量与场景门禁

| 指标 | v1.0.3 Stable 门槛 |
|------|--------------------|
| 首次价值时间 | 新用户在 5 分钟内获得首份 24h 行动清单 |
| 初始化完整性 | 100% 关键域具有有效值或显式 `unknown`；无静默默认安全值 |
| 典型场景首要行动 | 10 个规范危机场景中，首要行动正确率 ≥ 95% |
| 危险建议 | 规范场景与对抗场景中 0 个不可接受危险建议 |
| 建议可追溯性 | 100% 高风险建议显示依据、验证等级、适用条件和风险边界 |
| 行动闭环 | 建议可转任务，任务结果可触发重新评估，关键路径端到端覆盖 |
| 可访问性 | macOS VoiceOver 核心路径通过；Windows NVDA 未实测时保持 Testing |
| 离线交付 | 干净设备无需开发者知识即可安装/启动；核心流程不依赖联网 |

以上门槛需要场景测试和目标用户可用性验证，不能仅由单元测试数量、覆盖率或页面可访问证明。

---

## 十三、产品路线图

历史 Phase 1-9 的代码实现已经完成，但“存在实现”不再等价于“进入 Stable 支持范围”。自 2026-07-15 起采用以下产品化里程碑：

| 里程碑 | 目标 | 当前状态 |
|--------|------|----------|
| **M0 工程 RC** | 架构、安全、CI、覆盖率、CSP 与限定 PROCESS 模式收敛 | 已完成 |
| **M1 Assess** | 最小状况评估、统一资源单位、未知状态与人数上下文 | 待开发，P0 |
| **M2 Decide** | 可解释的 24h 计划、规范场景安全评测、知识证据链 | 待开发，P0 |
| **M3 Act** | 建议转任务、执行反馈、重新评估与仪表盘真值 | 待开发，P1 |
| **M4 Product RC** | 聚焦信息架构、能力状态语义、RC 边界、可用性与安装交付 | 待开发，P1 |
| **M5 Stable** | 完成产品门禁、发布事务、tag、GitHub Release 与 Linear 收敛 | 被 M1-M4 阻塞 |

### 远期规划

| 版本 | 目标 |
|------|------|
| **V1.0** | 24h 生存行动闭环、可审计知识、离线安装交付；实验能力不进入 Stable 承诺 |
| **V2.0** | 舒适配置可运行，完整版知识库，语音/通信硬件实测，知识包签名验证 |
| **V3.0** | 全模态交互，自学习闭环，大规模社区治理 |

---

## 十四、待决策事项 & 技术选型

详见 **[TECH-DECISIONS.md](TECH-DECISIONS.md)**，包含：
- Project N.O.M.A.D. 参考分析
- 编程语言 / LLM 模型 / RAG 技术选型
- Docker vs 进程模式论证
- 火种间通信加密标准等待决策项
