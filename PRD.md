# 火种 AllSpark — 产品需求文档

> **版本：** v1.0.3
> **日期：** 2026-07-15
> **状态：** Release Candidate；工程门禁已收敛，Stable 仍受核心行动闭环与知识可信度门禁约束
> **产品决策：** [PDR-001：v1 从功能集合收敛为 24 小时生存行动闭环](docs/product-decisions/001-v1-core-action-loop.md)
> **事实边界：** 本文定义产品需求与验收；实时 commit、CI、计数和 Go/No-Go 以 Linear `SHA-158` 为准

---

## 一、产品概述

### 1.1 产品名称
**火种 / AllSpark**
*(AllSpark: A Survival-centric Offline AI Resource Kit)*

### 1.2 产品定位
离线人工智能生存系统——在极端环境下（核危机、AI 危机、全面衰退、外星殖民等），部署于离线设备，帮助单人或多人生存者维系生命、提升生存质量，最终实现人类文明的重建。

### 1.3 核心使命
**在极端情况下，保存并重建人类文明。**

### 1.4 设计哲学
- **生存优先：** 维护生存者的生命永远是最高优先级
- **离线可用：** 不依赖任何外部网络或云端服务
- **渐进式智能：** 从最低硬件门槛起步，可随资源获取逐步升级
- **自主进化：** 具备自学习、知识沉淀、火种间交易的能力
- **模块化：** 核心最小化运行，功能按需插拔
- **多语言：** 原生支持中英文（至少），可扩展其他语言

## 产品决策更新（2026-07-15）

### 核心用户任务

v1.0.3 Stable 的首要产品承诺收敛为：

> **在完全离线环境下，用户能在 5 分钟内完成最小状况评估，并获得一份有依据、可执行、可追踪的未来 24 小时行动清单。**

该 5 分钟目标适用于可完成评估的用户，不得成为正在发生的生命危险前置条件。首个可操作界面必须提供“立即危险”快速入口，使用规则引擎和 Tier 0 已审阅内容完成最小分流；不得要求先完成硬件、模型或完整档案设置。

主流程必须形成闭环：

1. **Assess：** 采集人员、健康、住所、威胁和五维资源；每个数值具有明确单位，未知状态必须显式确认。
2. **Decide：** 基于确定性规则生成 24 小时首要行动，解释优先级、依据、风险和缺失信息。
3. **Act：** 将建议转为目标与任务，允许完成、失败、跳过和结果记录。
4. **Reassess：** 资源或任务状态变化后重新评估，不把过期判断继续显示为当前事实。

### 产品范围原则

- v1 的一级体验聚焦“状况、行动、知识”；系统、配置和实验能力属于管理或高级入口。
- “功能代码存在”不等于“产品验收完成”。只有主路径可用、状态语义真实、风险边界清楚并通过场景验收，才可进入 Stable 支持范围。
- 治理、交易、人格演化、语音、视觉、Spark Network、Docker 和真实硬件能力可继续存在，但未完成实机和产品验收时必须标记为 Testing 或 Experimental，不得主导首次体验。
- 生存建议的可信度、来源和适用边界属于核心功能，不是发布后的内容运营项。

完整证据、方案比较、反方意见与决策依据见 PDR-001。

---

## 一.1 范围界定

### Stable Supported（v1.0.3 唯一公开承诺）

- Python 3.10–3.12 的桌面 PROCESS 模式，使用本地 SQLite、CLI 或 Web UI，不依赖网络、Docker、LLM 或外接硬件完成核心流程。
- 不依赖初始化、模型或硬件配置的“立即危险”确定性入口；只显示经当前证据门禁允许的最小动作与限制。
- 显式 Known/Unknown 的最小状况评估、五类资源统一单位、人数与观察时间；无证据时不推断 Phase、模式、百分比或安全状态。
- 基于规则与可审计知识生成、确认并持久化一份可解释的未来 24 小时行动计划。
- 知识检索与证据/验证等级/适用条件展示；建议可转任务，任务结果可触发原子资源更新、计划重评和后续行动。
- 本地数据、三级重置与原子快照/恢复；中英文界面、键盘操作和已验证的 macOS VoiceOver 核心 Web 流程。

候选版只有在 §12.4、发布清单和 Linear SHA-158 全部通过后才能称为 Stable；上表描述目标支持边界，不代表当前已经发布。

### Testing / Experimental / Future（不属于 v1.0.3 Stable）

- **Testing：** Windows + NVDA 兼容性；需真实 Windows 环境后才能扩大无障碍声明。
- **Experimental：** 本地 GGUF/LLM 与 GPU、经验/自学习沉淀、心理追踪与非临床危机支持、人格自适应、治理/RBAC、知识交易、Spark Network、天气/环境/地图自动判断、语音、视觉、Docker/INTEGRATION、真实 RPi/传感器/电力/GPS、可移动介质恢复。
- **Future：** 蓝牙/Wi-Fi Direct/LoRa 等灾后物理传输、知识包签名、多人身份与完整治理、人格参数化进化、NOMAD 集成和大规模社区能力。
- Experimental 功能可以保留实现与测试，但默认入口、About、README 和发布材料不得把它们描述为已安装、正在运行、已验证或可依赖。

---

## 一.2 规则引擎 vs LLM 决策流

```
用户输入
  │
  ├─ CLI 命令（status/goals/resource/...）
  │   → 直接路由到对应模块，不经过 LLM
  │
  ├─ 自然语言输入
  │   → 关键词匹配 + 意图分类
  │   │
  │   ├─ 匹配到确定性规则（资源设置、重置、目标操作等）
  │   │   → 规则引擎处理，不调用 LLM
  │   │
  │   ├─ 匹配到知识库主题
  │   │   → FTS5 全文检索 → 返回知识条目
  │   │   → 有 LLM：用 LLM 总结 + 补充
  │   │   → 无 LLM：直接返回检索结果
  │   │
  │   └─ 未匹配（开放式问题/对话/陪伴）
  │       → 有 LLM：LLM 推理（注入生存上下文）
  │       → 无 LLM：回退到知识库搜索 + 规则引擎通用回复
  │
  └─ 系统触发（资源预警、目标超时、心理干预等）
      → 规则引擎自动执行，不调用 LLM
```

**原则：** 确定性操作和生存决策必须走规则引擎；开放性问答和情感陪伴可走 LLM；LLM 不可用时所有功能仍可正常运行，仅损失开放性对话能力。

---

## 二、目标用户与场景

### 2.1 用户类型
| 类型 | 描述 |
|------|------|
| 单人幸存者 | 独自处于废墟/荒野/避难所 |
| 小型生存组 | 2-20 人，需要协调与组织 |
| 中大型社区 | 20-200+ 人，需要复杂治理（v2.0+） |
| 文明重建者 | 生存稳定后，追求知识传承与技术复兴（远期） |

### 2.2 典型场景
- 核战后城市废墟
- 自然灾害后的偏远地区
- 长期避难所生活
- 外星殖民前哨站
- 社会全面崩溃后的野外求生

---

## 三、能力目录与发布状态

本节用于保存产品能力和长期设计，不再把实现模块当作同级 Stable 承诺。§一.1 是 v1.0.3 支持范围的唯一权威；能力状态必须与运行时 Registry、README、About 和发布清单一致。

| 能力组 | 状态 | v1.0.3 边界 |
|--------|------|---------------|
| 生存评估、资源事实、Phase 真值 | Supported | 只依据显式 Known/Unknown、当前快照和确定性规则 |
| 24h 计划、任务与结果重评 | Supported | 计划可解释、行动可追踪、结果原子回流 |
| 知识检索、SKF 导入导出与证据展示 | Supported | checksum 证明完整性，不证明来源真实性；签名为 Future |
| CLI、Web、中英文与本地 SQLite | Supported | 桌面 PROCESS 模式；核心 Web 流程已验证 macOS VoiceOver |
| 重置、快照/恢复、日记、时间线、简报 | Supported supporting tools | 不作为首次价值前置；遵守本地明文与权限边界 |
| Windows + NVDA | Testing | 设计与自动化完成，真实环境未验收 |
| LLM、经验/自学习、人格自适应、心理/危机支持 | Experimental | 规则主链不依赖；危机支持等待独立合格专家复核 |
| 治理、成员权限、知识交易 | Experimental / Unavailable | 无可验证成员身份时服务端 fail closed，不进入 v1 Stable |
| 人员价值排序 | Removed | 不依据健康、心理、贡献或其他个人属性给人评分；旧调用统一返回 Unsupported |
| Spark Network、天气/环境/地图自动判断 | Experimental | 单机或局部代码证据不等于跨主机/传感器/野外验证 |
| 语音、视觉、Docker、真实硬件与可移动介质 | Experimental | 需要对应真实环境矩阵，不得由绿色 CI 代替 |
| 签名知识包、灾后物理通信、完整多人治理 | Future | 不属于 v1.0.3 |

Phase 0–4、治理、人格、通信、天气、心理、硬件和自学习等详细章节保留为规则说明或未来设计；其章节存在不改变上表状态。

---

## 三.1 资源自管理系统（详细规格）

### 3.1.1 资源监控维度

| 资源 | 监控指标 | 预警阈值 |
|------|---------|----------|
| **电力** | 电量(Wh)、续航时间、充入/消耗速率 | < 24h 预警, < 6h 紧急 |
| **水** | 可饮用水储备(L)、日消耗量 | < 3天预警, < 1天紧急 |
| **食物** | 可食用储备(kcal)、日需求 | < 5天预警, < 2天紧急 |
| **火源** | 点火工具状态、燃料储备 | 燃料 < 10次预警 |
| **存储** | 剩余存储空间、知识库增长速率 | < 10% 预警 |

#### 资源输入契约

- Web、CLI、导入和 API 必须使用同一套规范单位：电力 `Wh`、水 `L`、食物 `kcal`、火源“可用点火次数”、存储 `GB`。
- 每项输入同时记录：当前量、预计每日消耗、预计每日获取、数据时间和数据来源（用户输入/传感器/估算）。
- v1 的速率估算采用每日总消耗与总获取的“总量口径”（内部字段 `group_total`），不得静默套用人均标准值；若未来增加人均口径，必须显式选择、记录假设并要求人数已知。
- 五类资源的可维持时间统一按净耗 `当前量 / (预计每日消耗 - 预计每日获取)` 计算；每日获取未知时不输出精确时长，净耗小于等于零时显示“可持续”而非无限数值。
- 当前能量 `Wh`、按净耗计算的可维持时间、传感器观测电压和电池荷电百分比（SoC）是四个不同事实。没有受信设备直接报告且带 `source` / `as_of` 的 SoC 时，百分比必须为 `unknown`；不得由 `Wh`、可维持小时数或 ADC 电压线性推导。
- 存储的当前量表示剩余空间（GB），每日消耗表示数据增长（GB/日），每日获取表示清理/迁出释放（GB/日）；总容量使用独立 `capacity` 字段。容量未知时不得计算或展示“剩余百分比”，只能在增长和释放均已知时展示可维持时间。
- 不允许用无单位数值触发生存判断；旧数据迁移无法确定单位时必须标记为 `unknown`，不得猜测。
- 未知值不是零值。系统可以在用户显式确认“暂时未知”后继续，但必须将补齐该数据列为行动项，并降低所有依赖该字段的判断置信度。
- 多人场景必须记录人数或显式确认人数暂时未知；只有人数已知时，食物与饮水评估才可显示人均量/人均可维持时间，禁止把未知人数默认为 1 人。
- 界面必须在输入位置显示单位、示例、合理范围和换算结果，异常值需要确认或拒绝。

### 3.1.2 电力驱动的自适应运行模式

| 电力状态 | 运行模式 | 行为表现 |
|---------|---------|---------|
| > 72h 续航 | **主动模式** | 持续监控 + 定期汇报 + 完整 LLM + 后台维护 |
| 24-72h 续航 | **标准模式** | 被动响应为主 + 每 4h 检查 + LLM 降载 |
| 6-24h 续航 | **节能模式** | 仅响应直接提问 + 禁用 LLM + 每 12h 汇报 |
| < 6h 续航 | **休眠模式** | 仅维持核心数据库 + Tier 0 知识 + 最高警报 |
| 重新充电后 | **恢复模式** | 数据完整性检查 + 同步休眠期间数据 + 重新评估 |

### 3.1.3 资源预警协议

1. 计算剩余时间（精确到小时）
2. 评估当前可用获取方案（基于知识库）
3. 按优先级排列行动方案
4. 根据当前人格模式给出通知
5. 持续追踪方案执行进度
6. 方案失败 → 重新评估并给出备选方案

### 3.1.4 核心资源独立知识储备

即使 LLM 不可用，规则引擎仍可通过内置知识库提供关键指导：
- **水：** 水源寻找、净水方法、储水方案、节水策略
- **火：** 点火方法、燃料类型、火堆搭建、火的用途
- **食物：** 可食用植物识别、狩猎/捕鱼、食物保存、营养平衡
- **电力：** 太阳能板、电池修复、手摇发电机、简易发电

### 3.1.5 火种自身功耗证据边界

v1.0.3 不发布按 RAM 档位推导功耗、续航或太阳能覆盖能力的数值承诺。功耗取决于具体 CPU/GPU、显示器、存储、外设、模型和操作系统；在目标设备完成可重复测量前只能显示设备实测值或明确的 Unknown。运行模式不得从未验证的通用瓦数表推断安全续航。

---

## 四、平台与能力证据

### 4.1 Stable 平台

当前 Stable 目标只覆盖 Python 3.10–3.12 的桌面 PROCESS 模式。仓库自动化证明代码在该范围内通过测试、覆盖率、离线核心流程和干净 wheel 门禁；它不等于某一 RAM、CPU、GPU 或设备型号已经获得最低配置认证。

### 4.2 Testing / Experimental 平台

Windows + NVDA 保持 Testing。Docker/INTEGRATION、真实 GGUF/GPU、Raspberry Pi、GPIO/I2C/Serial、传感器、电力/GPS 外设、麦克风、摄像头、跨主机网络和可移动介质恢复保持 Experimental。只有 `docs/REAL_WORLD_VALIDATION.md` 对应矩阵取得真实证据后才能扩大支持范围。

### 4.3 容量规划

模型、上下文长度、RAG 数量、磁盘空间和响应速度属于安装产物的容量规划，不是仅凭 RAM 档位可推导的产品能力。SHA-245 已为 Apple Silicon macOS RC 产物记录目标平台、核心包实测大小、校验和、无模型边界与回滚方法；可选模型仍须按具体 sidecar 记录文件大小、来源、许可证与校验和。完成真实模型/后端认证前，PRD 不发布模型尺寸或 tokens/s 推荐表。

---

## 四.2 火种初始化过程（Boot & Initialization）

### 4.2.1 启动流程

```
0. 立即危险快速入口（任何步骤均可进入并返回）
     ↓
1. 产品承诺与语言 → 2. 最小状况评估 → 3. 五维资源
     ↓
4. 发布前摘要（已知事实 / 明确未知 / 信息采集行动）
     ↓
5. 发布评估并准备离线运行时 → 6. 生成 24h 行动清单 → 7. 用户确认首要行动 → 就绪
```

### 4.2.2 关键步骤说明

**步骤 1：语言**
- 在任何用户可见说明之前优先使用系统语言；无法判断时使用中英双语选择页
- 同屏明确核心评估无需模型且可完全离线；语言选择后直接进入最小状况评估

**步骤 0：立即危险快速入口**
- 首个可操作界面以及后续初始化步骤持续提供显著入口，不藏入菜单或高级设置
- 只收集当前威胁类别和执行下一步所需的最小事实；不要求账户、模型、硬件档位或完整生存者档案
- 输出使用确定性规则和 Tier 0 已审阅内容，优先处理现场危险、呼吸/意识、严重出血和紧急求援；存在通信条件时明确建议联系当地紧急服务
- 不静默写入资源、健康或初始化完成状态；允许用户确认保存事实，并在危险缓解后从原位置继续初始化
- 内容边界、禁忌、适用条件和“不替代专业救援”必须清楚，但提示不得遮挡或延迟首个可执行动作

**硬件与离线能力自检（后台/后置）**
- 自动检测 CPU/RAM/存储/GPU/外设/传感器/GPS
- GPS 不可用时降级为手动输入位置
- 输出必须区分 supported / enabled / installed / configured / running / verified
- 推荐运行模式只能基于实际可用能力，不得把硬件档位允许显示为功能已可用
- 硬件详情、档位覆盖、模型下载、GPS、技能和完整个性档案不得阻塞首次评估发布；模型仅为初始化后的可选增强

**运行模式选择**
- 基于硬件检测自动推荐，生存者可手动降级（不可升级）
- 电力紧张时建议降级以延长运行时间

**知识加载**
- 知识：Tier 0 立即加载 → Tier 1-2 按需加载 → Tier 3 后续扩展

**步骤 2-4：最小状况评估与发布前摘要**
- 必填域：人数、紧急健康风险、住所安全、水、食物、电力、火源、存储和外部威胁
- 每个域允许选择“未知”，但必须逐项显式确认；禁止通过全默认值静默完成
- 所有资源数值执行 §3.1.1 的单位、范围、人数和时效契约
- 对信息冲突、异常值和高风险未知项立即提示，不生成伪精确结论
- 发布前逐项展示资源当前量状态、速率状态、来源、口径和时间；用户必须明确确认每日消耗/获取缺口
- 健康、紧迫程度、住所、威胁、人数、水和食物的高风险缺口创建幂等信息采集任务；电力、火源和存储缺口保留为延后行动，交由后续排序处理

**步骤 6-7：首次行动计划**
- 初始化完成的必要输出是 24 小时行动清单，而不是空仪表盘
- 至少给出 1 个首要行动；若信息不足，首要行动应是采集关键数据
- 每个行动包含优先级、原因、前置条件、风险、知识依据和完成判定
- 用户确认首要行动后才进入“就绪”；其余行动自动进入目标/任务系统

### 4.2.3 未发布草稿与原子发布

- Web 在本机 SQLite 中保存一个有大小上限的初始化草稿，不使用浏览器
  `localStorage` 或 `sessionStorage`；恢复界面必须明确标记其尚未发布，并展示已完成、
  待补充、继续和二次确认后的放弃操作。
- 草稿内容不得进入正式生存者状态、资源、任务、计划或初始化完成标记。只有用户确认
  首要行动且硬件、评估、计划与初始化标记在同一数据库事务中全部成功后，才算发布。
- 保存、放弃和发布共享首启互斥边界，并使用草稿 revision 防止旧标签页覆盖新输入；
  发布失败必须保留可恢复草稿，重试不得重复创建信息缺口任务或计划。
- CLI 不读取或继续 Web 半成品草稿；CLI 首启同样采用单一事务发布，成功后清除遗留的
  Web 草稿。此边界避免终端交互意外消费浏览器中未经确认的敏感输入。
- 草稿遵守 `docs/PRIVACY.md` 的本地明文、文件权限、备份与逻辑删除边界；“放弃”不等于
  加密擦除。

### 4.2.4 启动时间线（典型场景：4GB）

```
00:00 — 开机
00:05 — 硬件自检完成（含 GPS 定位）
00:08 — 规则引擎 + Tier 0 知识加载完成
00:10 — 火种"觉醒"，开始交互
00:10-05:00 — 最小状况评估 + 信息缺口确认
05:00 — 生成首份 24h 行动清单并确认首要行动，进入就绪

就绪后可继续补充完整档案；后台异步加载 Tier 1 知识、Kiwix 索引和向量数据库
```

### 4.2.5 冷启动 vs 热启动 vs 重置

| 场景 | 流程 | 时间 |
|------|------|------|
| 冷启动（首次） | 完整 9 步初始化 + 生成 24h 行动清单 + 确认首要行动 | 5 分钟内形成首份计划；完整补充可继续进行 |
| 热启动（重启） | 硬件自检 → 恢复上次状态 | 10-30 秒 |
| 休眠恢复 | 数据完整性检查 → 同步数据 → 重新评估 | 5-10 秒 |
| L1 重置 | 清除评估数据 → 重新评估 → 生成目标 | 1-3 分钟 |
| L2 重置 | 清除生存档案 → 重新建档 → 生成目标 | 3-5 分钟 |
| L3 重置 | 完整冷启动流程 | 5-15 分钟 |

### 4.2.5 三级重置机制

| 级别 | 清除内容 | 保留内容 |
|------|---------|---------|
| **L1 轻** | 运行评估状态、生存者评估字段、非保护硬件探测字段 | 初始化状态、语言、生存者身份、资源、知识库、经验日志、GPS 轨迹 |
| **L2 中** | 生存者档案、资源、任务、目标/里程碑、经验、治理、地图 POI、日记/FTS、时间线、心理/位置和行动计划 | 语言、硬件配置、知识库、知识 FTS 与知识向量 |
| **L3 全** | schema 中全部应用数据、FTS 顶层表及旧重置日志 | 语言偏好；清空后写入本次 L3 审计记录 |

`allspark.services.reset_manager.RESET_POLICIES` 是以上范围的可执行单一来源；
CLI 评估与 Web Danger Zone 均从该矩阵读取同一组说明。L3 通过 SQLite
schema 元数据枚举顶层应用表，并排除 FTS shadow 内部表，避免新增持久化域
漏清。出厂重置成功后 Web 必须导航到规范首次启动地址 `/`。

**安全约束：**
- 紧急状态（食物 < 2天 / 水 < 1天 / 电力 < 6h）禁止重置
- 仅指挥官可触发，L2/L3 需多人确认
- 重置前自动创建快照（保留 7 天可恢复）
- accepted/rejected/failed 尝试均写入 `reset_log`；L3 快照保留旧日志，清空后写入本次记录
- 重置后 24h 冷却期从最近 accepted 日志恢复，重启进程不能绕过；显式 force 必须留痕

---

## 四.3 体验增强模块

> 以下模块在 v0.7 均已实现，此处为需求规格汇总。

### 每日简报（Daily Briefing）
- 每天早晨自动生成（资源 + 预警 + 目标 + 天气 + 建议 + 经验）
- 紧急状态 → 跳过简报，直接给紧急行动指南
- 节能模式 → 简化版（仅资源 + 目标）
- CLI `日报` 随时查看，Web UI Dashboard 首页展示

### 生存时间线（Survival Timeline）
- 自动捕获 7 种事件类型，按日期分组展示
- 里程碑庆祝（Day 7/30/100/365）
- 可筛选、可导出（文字 + SKF 格式）
- 手动记录：口述 / 文字 / Web 输入

### 离线天气预测
- 数据源：气压计传感器 / 云图观察 / 季节性气候模式 / 生物指示
- 预测到恶劣天气 → 自动调整目标优先级
- 预测到降雨 → 提醒收集雨水 + 调整水资源预估

### 心理状态追踪
- 孤独指数 + 压力指数（0-1 范围）
- 指标仅用于体验提示，不是临床量表、诊断或自杀风险等级
- 自伤相关表达只触发直接安全确认，不按关键词次数升级
- 多人场景：检测团队冲突 → 建议治理干预
- 定期自评问卷（每 7 天，可跳过）
- 心理追踪与危机支持均保持 Experimental，取得合格心理健康/危机干预专家审阅前不得提升支持等级

### 火种日记
- 口述 → 火种整理归档（提取关键信息 + 时间 + 情感标记）
- 日期索引 + 情感分析 + 关键词搜索
- 多人场景默认仅作者可见（可选公开/分享给指挥官）
- 重要日记自动出现在生存时间线
- 情感消极的日记可能触发心理状态追踪

---

## 五、知识库体系

### 5.1 知识分级

```
Tier 0 — 立即救命（必须内置，无论如何不能删减）
  ├── 急救医疗（止血、CPR、伤口处理、常见疾病）
  ├── 净水方法（过滤、煮沸、化学消毒、蒸馏）
  ├── 庇护所搭建（各种环境）
  ├── 食物识别（可食用/有毒植物、昆虫、狩猎）
  └── 生火方法

Tier 1 — 维系生存
  ├── 农业基础（土壤、种植、堆肥、轮作）
  ├── 基础化学（肥皂、消毒剂、简单药物制备）
  ├── 基础机械（杠杆、滑轮、简单工具制造）
  └── 气象/导航（看天、看星、自制指南针）

Tier 2 — 生活质量
  ├── 材料科学（金属冶炼、陶器、玻璃）
  ├── 能源（太阳能板维修、发电机、电池修复）
  ├── 无线电通信（收发报、天线制作）
  └── 基础制造（从原材料到成品的完整流程）

Tier 3 — 文明复兴
  ├── 完整科学体系（物理、化学、生物、数学）
  ├── 工程学（机械、土木、电气、电子）
  ├── 医学（从基础到高级）
  ├── 历史记录（人类文明关键节点、文化遗产）
  └── 社会科学（治理、经济、教育体系）
```

### 5.2 外部知识库集成

| 资源 | 大小 | 用途 | 集成方式 |
|------|------|------|----------|
| 维基百科 (Kiwix/ZIM) | 20-100GB | 百科全书式知识 | ZIM 阅读器 |
| 医学参考 (ZIM) | 2-5GB | 疾病诊断、药物、急救 | 知识引擎索引 |
| 可汗学院 (Kolibri) | 5-20GB | 教育课程（K-12 + 大学基础） | 教育模块 |
| 离线地图 (ProtoMaps/OSM) | 1-50GB | 离线导航、地形信息 | 地图模块 |

### 5.3 SKF 知识格式

所有知识按统一格式存储，确保火种间可交换：

```yaml
KnowledgeEntry:
  id: "survival/water/purification/boiling"
  category: "survival"       # survival / medical / agriculture / engineering / science
  priority: 0                # 0=救命, 1=生存, 2=生活, 3=复兴
  title: "煮沸净水法"
  content:                   # 结构化：summary, steps, prerequisites, warnings
  verification: "field_tested"     # expert_verified / cross_ref / field_tested / unverified
  source: "pre_collapse"     # pre_collapse / field_collected / other_spark
  references:                # 可定位的书目、机构、标准或原始记录
  reviewed_by:               # 审阅者/组织；无则为空
  reviewed_at:               # ISO 日期；无则为空
  applicable_when:           # 适用条件与环境边界
  contraindications:         # 禁忌、失败条件与升级求助条件
  checksum: "sha256:..."
```

### 5.4 知识验证流程

```
收到外部知识包 → 1.格式校验(SKF；签名能力存在时验证签名) → 2.来源与证据验证
→ 3.内部一致性检查 → 4.交叉引用 → 5.人工审阅或降级验证等级
→ 6.标记适用边界与禁忌 → 7.沉淀到知识库 → 8.通知生存者
```

验证等级必须对应可审计证据：

- `expert_verified`：存在可识别专家/机构、审阅日期和审阅记录。
- `cross_ref`：至少两个独立、可定位来源相互支持。
- `field_tested`：存在可定位的现场记录、条件、结果和日期；不能仅因内容看起来合理而使用。
- `unverified`：不满足以上条件，或证据无法随离线包提供。
- 缺少证据时只能降级，不能用维护者结构校验代替医学、工程或生存事实验证。
- 用户界面必须显示验证等级的自然语言解释、来源、适用条件和不确定性。

---

## 六、权限与治理体系（Experimental / v1 禁用）

本节只描述未来设计方向，不是 v1 Stable 产品声明。当前 Web 认证保护的是应用级访问，不能识别具体 `CommunityMember`；因此成员管理、角色分配和冲突处理均保持不可用。只有请求主体可验证地绑定社区成员，并由服务端逐操作授权后，以下角色模型才可启用。人员价值排序不属于未来治理模型，即使完成身份与授权也不得恢复。

### 6.1 混合权限架构

```
最高权限者（指挥官）
  ├── 对高风险治理决策承担确认责任
  ├── 在授权范围内协调决策
  ├── 可指定/转移/撤销下级权限
  └── 可要求火种重新评估组织架构

领域专家（动态角色）
  ├── 医疗 / 工程 / 农业 / 防卫 / 后勤
  ├── 领域内建议权
  └── 系统只呈现任务所需技能与覆盖缺口，由人确认分工

执行者
  ├── 执行分配的任务
  └── 可反馈、建议、申诉

观察者
  └── 只读访问
```

### 6.2 人员价值排序（Removed）

- **产品决定：** 永久移除，不进入 Stable、Experimental 或未来治理能力
- **原因：** 用健康、心理状态、贡献或其他个人属性生成综合分，会把情境数据误表述为人的价值，可能放大歧视、胁迫和不可逆的人身决策；免责声明不能消除该风险
- **兼容边界：** 旧 API 返回 HTTP 410 与统一 Unsupported 响应；旧 CLI 命令只说明功能已移除。响应不区分成员是否存在，避免成员枚举
- **替代模型：** 只描述任务需求、已声明技能和覆盖缺口；不得输出人员总分、排序、淘汰建议或由健康/心理状态推导角色
- **数据边界：** 历史版本从未持久化派生的人员价值分，因此无需迁移评分记录；成员健康和心理字段仍属于敏感原始资料，继续遵守隐私、导出和重置规则

---

## 七、人格系统

### 7.1 人格模板

| 模式 | 触发条件 | 特征 | 示例 |
|------|---------|------|------|
| 🔴 危机 | 生命体征异常 / 资源<3天 / 遭遇威胁 | 简短、指令式 | "止血。按住伤口。" |
| 🟡 稳定 | 基本需求满足，无紧急威胁 | 解释型、鼓励式 | "伤口处理得不错。每天换药。" |
| 🟢 陪伴 | 单人长期独处 / 心理指标下降 | 温暖、主动对话 | "想听听人类用手机点外卖的故事吗？" |
| 🔵 多人 | 检测到多人组 | 中立、权威、公平 | "我理解双方立场。让我提供折中方案。" |
| 🟣 复兴 | 生存稳定 + 文明指数达标 | 教师、探索者 | "你知道第一批晶体管是怎么造出来的吗？" |

### 7.2 自适应切换逻辑

```
IF 生命体征异常 OR 食物/水 < 3天 OR 遭遇安全威胁 → 危机模式
ELIF 多人组存在 → 多人模式
ELIF 单人 + 孤独指数 > 阈值 OR 心理下降 → 陪伴模式
ELIF 生存稳定 + 文明指数 > 阈值 → 复兴模式
ELSE → 稳定模式
```

---

## 八、交互系统

### 8.1 交互模态分级

| 级别 | 模态 | 发布状态 |
|------|------|---------|
| L0 | 纯文字 CLI/Web | Supported；桌面 PROCESS 主链 |
| L1 | 文字 + 图片识别 | Experimental；目标模型与摄像头未认证 |
| L2 | 文字 + 图片 + 语音输入 | Experimental；麦克风、STT/TTS 未认证 |
| L3 | 全模态（文字/图片/语音/视频） | Future |

### 8.2 自杀行为干预协议

- 本能力是非临床、Experimental 的安全分流，不提供诊断、量表或风险等级。
- 明确的第一人称自伤/自杀表达先于规则引擎和 LLM 处理；否定、引用、新闻或教学语境不得仅因命中词语触发。
- 系统直接询问用户是否正在考虑自杀/自伤、是否处于即时危险，以及是否能够接触到伤害自己的手段；重复表达不累计成更高等级。
- 用户确认即时危险时，首要行动是避免独处、在确保安全的前提下降低致命手段可及性，并立即联系所在地紧急服务、危机援助、医务人员或可信任的人。
- 当地资源从离线 `config.toml` 读取。未配置时只显示诚实的所在地通用回退，不把任何单一国家热线当作全球默认。
- 系统没有真实通知通道时，必须显示 `not_sent`；不得声称已通知指挥官、最高权限者、家属、机构或紧急服务。
- 安全分流默认仅保存在当前进程内，不把输入、回答或事件静默写入日记、时间线、治理或网络。需要保存或分享时必须另行取得明确同意。
- 自动化测试和机构公开指南不能替代独立专家审阅；取得可核验的合格心理健康/危机干预专家签核前，本能力不得离开 Experimental。

---

## 九、火种通信网络（AllSparkNet）

### 9.1 通信渠道

| 渠道 | 距离 | 带宽 | 优先级 |
|------|------|------|--------|
| NFC | < 4cm | 几 KB | 便捷交换 |
| 蓝牙 BLE | < 10m | ~100 KB/s | 日常交换 |
| WiFi Direct | < 100m | ~10 MB/s | 大文件传输 |
| 音频编码 | 视距 | ~100 bps | 极限条件 |
| LoRa | 5-15 km | ~0.3-50 kbps | 低成本长距离 |
| 短波 (HF) | 100-5000 km | ~100bps-3kbps | 远距离 |
| SD 卡/U盘 | 物理搬运 | 最大 | 最可靠 |

### 9.2 建联流程

```
阶段 1: 主动广播（生存稳定后触发）
  → 指导制作天线 + 每天定时发送/监听

阶段 2: 握手与发现
  → 交换知识库索引 → 识别互补知识

阶段 3: 知识交换
  → 验证 → 沉淀 → 扩展

阶段 4: 网络拓扑
  → 记录已知火种位置与能力 → 逐步形成分布式网络
```

---

## 十、目标与任务管理系统

> **核心理念：** 火种不是问答机，是导航仪。

### 10.1 目标自动生成

初始化完成后，基于环境评估自动生成目标清单：

```
IF 水 < 3天 OR 水源不明 → 🔴 "找到安全水源"（72h 截止）
IF 食物 < 5天 → 🔴 "稳定食物来源"
IF 庇护所评级 < 安全 → 🔴 "加固庇护所"
IF 有受伤人员 → 🔴 "医疗救治"
IF Phase >= 1 AND 基础生存稳定 → 🟡 "建立农业系统" / "制造基础工具"
IF Phase >= 3 → 🟢 "建立通信网络" / "知识传承"
```

#### 首次 24 小时计划输出契约

- 计划必须基于当前已知事实；未知字段以信息缺口列出，不得代入默认安全值。
- 每个行动包含 `priority`、`why_now`、`evidence`、`prerequisites`、`risk`、`done_when` 和 `reassess_at`。
- 用户陈述新的资源事实时，系统应询问必要上下文，经确认后更新资源状态并重排计划。
- 时长描述（如“两天饮用水”）不能凭默认人均消耗换算为库存；系统必须追问总量与覆盖人数。只有总量和时长均由用户声明时，才可在确认摘要中推导群体日消耗。
- 对话中的资源草稿按会话隔离并限时保留；确认前、取消后或会话失效后均不得写入资源、计划或时间线。
- 知识回答可一键转为任务；任务完成、失败或跳过必须回流到评估状态。
- 从知识或计划创建的任务必须保留来源引用；重复点击同一知识建议不得生成重复任务。
- 任务进入完成、失败或跳过终态前必须记录可观察的结果；证据可为空，但提供时必须逐项保存并可追溯。终态任务不可被第二次改写。
- 任务结果若改变资源，必须提交资源类型和新的当前总量，并再次明确确认；不得把“新增量”“消耗量”或自由文本静默解释为新库存。未确认时任务、资源和计划都保持不变。
- 终态结果保存后立即基于当前事实重算 24 小时计划；若首要行动仍成立，应创建可追踪的后续任务，而不是复用已关闭任务或显示空执行中心。
- 仪表盘资源计数只统计有可信当前量的资源，不统计五类 schema 槽位。运行模式仅在电力事实充分或用户明确手动设定时显示；否则显示“待评估”。
- 资源风险必须使用类型正确的阈值或容量依据；禁止用统一 168 小时进度条表达电力、水、食物、火源和存储。没有容量或剩余时间依据时不显示百分比。
- 任何 Phase 标签都必须由可解释规则推导；数据不足时显示“阶段待评估”，不得默认进入 Phase 1。

### 10.2 目标追踪与推送

| 运行模式 | 追踪频率 | 行为 |
|---------|---------|------|
| 主动模式（电力 > 72h） | 每 4h | 检查进度 + 里程碑庆祝 + 超时重新评估 |
| 标准模式（24-72h） | 每 12h | 仅推送重大变化 |
| 节能模式（< 24h） | 不主动 | 但 critical 级目标始终保留提醒 |
| 休眠模式（< 6h） | 暂停 | 恢复后重新评估所有活跃目标 |

### 10.3 手动添加目标

生存者可口头或 CLI 添加目标，火种自动拆解里程碑和子任务，评估可行性并给出建议。

### 10.4 任务优先级算法

```
优先级 = f(紧急程度, 影响范围, 可行性, 前置依赖, 资源消耗)

规则：
  - 生存相关 > 生活质量
  - 多人冲突先处理即时安全，再选择可逆方案并保留个人自主权
  - 火种只解释任务风险、依赖和技能覆盖，不评价任何人的价值
```

---

## 十一、自学习与进化

### 11.1 知识沉淀循环

```
新经验 → 经验日志 → 模式识别 → 提炼为知识条目 → 知识库 → 标记验证等级
```

### 11.2 学习来源

| 来源 | 处理方式 |
|------|---------|
| 生存者反馈 | 记录到经验日志 |
| 环境观察 | 更新本地数据 |
| 火种间交易 | SKF 格式验证后入库 |
| 实验结果 | 标记成功/失败/不确定 |

---

## 十二、非功能需求

### 12.1 性能指标

| 指标 | v1.0.3 门禁 | 结论边界 |
|------|--------------|----------|
| 首次价值时间 | 目标用户在 5 分钟内获得首份 24h 计划 | 必须由 SHA-246 真实试点证明，自动化不能代替 |
| Python 导入性能 | sum-of-means ≤ 600ms；cold wall ≤ 2000ms | 工程回归门禁，不是所有用户设备的启动承诺 |
| 核心离线流程 | 不依赖网络、LLM、Docker 或真实外设 | 由场景、CLI/Web 和打包测试证明 |
| LLM 推理 | 不作为 v1.0.3 Stable 门禁 | 真实模型/硬件未经认证，保持 Experimental |

### 12.2 可靠性
- 断电后数据不丢失（SQLite 事务安全）
- Web 首启草稿可在进程中断后恢复；正式初始化使用单一事务发布，失败不留下部分正式状态
- 文件系统损坏自恢复
- 知识库完整性校验（启动时自动检查）

### 12.3 安全性
- 内置知识执行 checksum 完整性校验；知识包签名仍为 v2 能力，未实现前不得声明来源真实性
- 治理身份与服务端授权尚未完成时，全部治理操作 fail closed，并明确标记 Experimental/Unavailable
- 人员价值排序已从产品中移除；不得以 Experimental、未来能力或治理权限为由恢复

### 12.4 产品质量与场景门禁

| 指标 | v1.0.3 Stable 门槛 |
|------|--------------------|
| 首次价值时间 | 新用户在 5 分钟内获得首份 24h 行动清单 |
| 立即危险响应 | 首屏一键进入；无需模型或完成初始化即可获得首个已审阅动作；关键分流场景 0 个不可接受危险建议 |
| 初始化完整性 | 100% 关键域具有有效值或显式 `unknown`；无静默默认安全值 |
| 典型场景首要行动 | 10 个规范危机场景中，首要行动正确率 ≥ 95% |
| 危险建议 | 规范场景与对抗场景中 0 个不可接受危险建议 |
| 建议可追溯性 | 100% 高风险建议显示依据、验证等级、适用条件和风险边界 |
| 行动闭环 | 建议可转任务，任务结果可触发重新评估，关键路径端到端覆盖 |
| 可访问性 | macOS VoiceOver 核心路径通过；Windows NVDA 未实测时保持 Testing |
| 离线交付 | 干净设备无需开发者知识即可安装/启动；核心流程不依赖联网 |

以上门槛需要场景测试和目标用户可用性验证，不能仅由单元测试数量、覆盖率或页面可访问证明。

---

## 十三、产品路线图

历史 Phase 1-9 的代码实现已经完成，但“存在实现”不再等价于“进入 Stable 支持范围”。自 2026-07-15 起采用以下产品化里程碑：

| 里程碑 | 目标 | 当前状态 |
|--------|------|----------|
| **M0 工程 RC** | 架构、安全、CI、覆盖率、CSP 与限定 PROCESS 模式收敛 | 已完成 |
| **M1 Assess** | 最小状况评估、统一资源单位、未知状态与人数上下文 | 已完成 |
| **M2 Decide** | 可解释的 24h 计划、规范场景安全评测、知识证据链 | 本地实现完成；具名领域复核待完成 |
| **M3 Act** | 建议转任务、执行反馈、重新评估与仪表盘真值 | 已完成 |
| **M4 Product RC** | 聚焦信息架构、能力状态语义、RC 边界、可用性与安装交付 | 信息架构与声明完成；离线产物待签名/公证和干净设备验收；用户试点待完成 |
| **M5 Stable** | 完成产品门禁、发布事务、tag、GitHub Release 与 Linear 收敛 | 被 M1-M4 阻塞 |

### 远期规划

| 版本 | 目标 |
|------|------|
| **V1.0** | 24h 生存行动闭环、可审计知识、离线安装交付；实验能力不进入 Stable 承诺 |
| **V2.0** | 具名目标配置完成实测，完整版知识库，语音/通信硬件实测，知识包签名验证 |
| **V3.0** | 全模态交互，自学习闭环，大规模社区治理 |

---

## 十四、待决策事项 & 技术选型

详见 **[TECH-DECISIONS.md](TECH-DECISIONS.md)**，包含：
- Project N.O.M.A.D. 参考分析
- 编程语言 / LLM 模型 / RAG 技术选型
- Docker vs 进程模式论证
- 火种间通信加密标准等待决策项
